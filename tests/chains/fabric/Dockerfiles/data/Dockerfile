ARG ALPINE_VER=3.13

FROM alpine:${ALPINE_VER}

ARG MSP_CONF_DIR_PATH
ARG CONNECTION_PROFILE_DIR_PATH

WORKDIR /data

COPY ${MSP_CONF_DIR_PATH} ./organizations
COPY ${CONNECTION_PROFILE_DIR_PATH} ./connection-profile
